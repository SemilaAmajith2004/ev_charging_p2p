import 'package:flutter/material.dart';

class AppColor {
    // Backgrounds & Surfaces
    static const ColorPrimary = Color(0xFF0F172A);
    static const ColorPrimaryDark = Color(0xFF1E293B);
    static const ColorPrimaryLight = Color(0xFF334155);

    // Accent Neon Colors (EV Aesthetic)
    static const Color neonGreen = Color(0xFF00F5D4);
    static const Color neonBlue = Color(0xFF00BBF9);
    static const Color solarAmber = Color(0xFFF59E0B);
    static const Color alertRed = Color(0xFFF43F5E);

    // Text Colors
    static const Color textPrimary = Color(0xFFF8FAFC);
    static const Color textSecondary = Color(0xFF94A3B8);

}